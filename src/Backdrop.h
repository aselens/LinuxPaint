#pragma once

#include <QColor>
#include <QWidget>

// Подложка окна: ровная заливка плюс мягкие цветные отсветы по краям.
//
// Именно так выглядит фон в Windows 11: не переливающийся целиком градиент,
// а спокойная тёмная поверхность с подсвеченными краями. Сейчас очагов два —
// сильный из левого нижнего угла и слабее от правого края на середине
// высоты. Оба растворяются, не доходя до центра, поэтому читаются как
// отсвет, а не как заливка двумя цветами.
//
// Рисуется виджетом, а не фоном из таблицы стилей: так пятно ложится на всю
// площадь окна разом, включая область вокруг холста. Слои поверх него
// полупрозрачны, и свечение видно сквозь них.
class Backdrop : public QWidget
{
    Q_OBJECT

public:
    explicit Backdrop(QWidget *parent = nullptr);

    // base — ровный фон, glow — цвет свечения вместе с его силой в альфе.
    void setColours(const QColor &base, const QColor &glow);

protected:
    void paintEvent(QPaintEvent *event) override;

private:
    QColor m_base;
    QColor m_glow;
};
